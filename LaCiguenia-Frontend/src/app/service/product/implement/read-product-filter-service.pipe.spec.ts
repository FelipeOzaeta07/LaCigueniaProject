import { ReadProductFilterServicePipe } from './read-product-filter-service.pipe';

describe('ReadProductFilterServicePipe', () => {
  it('create an instance', () => {
    const pipe = new ReadProductFilterServicePipe();
    expect(pipe).toBeTruthy();
  });
});
